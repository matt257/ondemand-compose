version: '3.8'

services:
  ondemand:
    build:
      context: ./ondemand
    ports:
      - "80:80"
      - "443:443"
    restart: unless-stopped

volumes:
  ldap_data:
  ldap_config:
